//- mixin script(file)
//-  - for ( i = 0 ; i < file.length; i++ ) {
//-     script(src=file[i])
//-  - }

//- mixin list(class, ...name)
//-  - for (i= 0 ; i < name.length; i++) 
//-        li(id=class) = name[i]

mixin list(class ,...items)
 ul(class=class)
  each item in items
    li= item


